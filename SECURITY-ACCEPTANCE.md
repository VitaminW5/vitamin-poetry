# 维生素 V1.5 Stable · 上线验收清单

## 1. 公开前台

- 打开 `/`，首页正常。
- 三个文集均能打开，数量应为 39 / 9 / 49。
- 随机抽查至少 3 篇作品，海报、正文背景和正文均正常。
- 页脚不再显示 Studio / 后台入口。
- 直接访问 `/studio` 仍能进入登录页。

## 2. 后台登录

- 使用 Cloudflare 中的 `VITAMIN_ADMIN_PASSWORD` 登录。
- 登录成功后能看到全部 97 篇作品。
- 不要连续故意输错 5 次密码；V1.5 会对同一 IP 的失败登录进行 15 分钟限流。
- Session 最长有效期为 24 小时，过期后需要重新登录。

## 3. D1 持久化测试

1. 选择一篇作品，在正文末尾临时增加一个测试标点。
2. 点击“保存修改”。
3. 刷新前台，确认修改已经出现。
4. 退出 Studio，再重新登录，确认修改仍然存在。
5. 把测试标点删掉并再次保存。

只要第 4 步仍然存在，就说明 D1 持久化链路正常。

## 4. 安全检查

- GitHub 仓库中不得出现真实的 `VITAMIN_ADMIN_PASSWORD`。
- GitHub 仓库中不得出现真实的 `VITAMIN_SESSION_SECRET`。
- 两个 Secret 只存放在 Cloudflare → Pages → Settings → Variables and secrets。
- `/studio` 不应出现在前台任何导航或页脚中。
- `/studio` 和 `/api/*` 已设置 `noindex`；这只是降低搜索发现概率，真正安全性仍由密码、Session、CSRF 与限流提供。

## 5. 推荐的稳定版本操作

当以上检查全部通过后，在 GitHub 上给当前提交打一个 tag：`v1.5-stable`。以后做视觉或功能修改时，从这一版本继续，不要再回退到 V1.4.x。
