# V1.5 部署方式

Cloudflare 上现有的 D1、DB Binding、VITAMIN_ADMIN_PASSWORD、VITAMIN_SESSION_SECRET 都不要改。

## 最简单更新方式

1. 解压 `vitamin-v1.5-security-patch.zip`。
2. 将包内文件复制到本地 `vitamin-poetry` Git 仓库，覆盖同名文件。
3. GitHub Desktop 中填写 Summary：`V1.5 security hardening`。
4. `Commit to main`。
5. `Push origin`。
6. Cloudflare Pages 会自动触发新部署。
7. 等 Deployment 状态为 `Success` 后，按 `SECURITY-ACCEPTANCE.md` 验收。

本次升级不迁移 D1 表结构，也不会覆盖已经在 D1 中修改过的作品。
